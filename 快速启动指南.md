# 🚀 PromptLab MCP Server 快速启动指南

## 📦 一键安装

```bash
# 1. 进入项目目录
cd e:\User\MCP\PromptLab

# 2. 运行自动安装脚本
python setup_and_start.py
```

## ⚡ 快速启动

### 方法一：Python 脚本（推荐）
```bash
python start_mcp_server.py
```

### 方法二：PowerShell (Windows)
```powershell
.\start_promptlab_mcp.ps1
```

### 方法三：批处理文件 (Windows)
```cmd
start_promptlab_mcp.bat
```

## 🔧 配置 MCP 客户端

### Trae AI 配置
1. 复制 `mcp_trae.json` 内容
2. 修改路径为你的实际路径
3. 在 Trae AI 中加载配置

### 其他 MCP 客户端
1. 使用 `mcp.json` 配置文件
2. 根据客户端要求调整配置

## 🎯 启动 MLflow（可选）

**在新的终端窗口中运行：**

```bash
# Windows PowerShell
cd e:\User\MCP\PromptLab
.\venv\Scripts\Activate.ps1
mlflow server --host 127.0.0.1 --port 5000
```

```bash
# Windows CMD
cd e:\User\MCP\PromptLab
venv\Scripts\activate
mlflow server --host 127.0.0.1 --port 5000
```

## 📋 验证安装

### 1. 测试 MCP 服务器
```bash
python test_mcp.py
```

### 2. 检查 MLflow（如果启动了）
访问: http://127.0.0.1:5000

### 3. 注册示例提示词
```bash
python register_prompts.py
```

## 🛠️ 可用工具

1. **optimize_query**: 优化用户查询
2. **list_prompts**: 列出所有提示词模板
3. **reload_prompts**: 重新加载提示词

## ⚠️ 常见问题快速解决

### 依赖包错误
```bash
pip install numpy==1.26.4 pandas==2.2.3
```

### 权限问题 (Windows)
```powershell
# 以管理员身份运行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 虚拟环境问题
```bash
# 重新创建
rmdir /s venv  # Windows
python -m venv venv
pip install -r requirements.txt
```

## 📞 需要帮助？

1. 查看详细文档：`使用说明.md`
2. 启用调试模式：设置 `PROMPTLAB_DEBUG=true`
3. 检查终端输出的错误信息

---

**🎉 恭喜！你的 PromptLab MCP Server 已准备就绪！**