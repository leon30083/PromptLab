好的，这是为您精心准备的、完整的 **PromptLab (Vertex AI & Gemini 2.5 Pro) 测试流程文档**。

请按照以下步骤，一步一步地进行操作和验证，以确保您的项目已经完全配置正确并可以正常工作。

-----

### **PromptLab 端到端测试流程文档**

**目标**: 验证 `PromptLab` 系统能够通过 Google Cloud Vertex AI 成功调用 `gemini-2.5-pro` 模型，并完整处理从客户端发起的请求。

#### **一、 前提条件与环境检查**

在开始测试前，请务必确认以下所有准备工作均已完成：

1.  **Google Cloud 设置**:

      * 您拥有一个 Google Cloud Platform (GCP) 项目，并且已在该项目中**启用了 Vertex AI API**。
      * 您已经通过 `gcloud` 命令行工具成功登录并设置了配额项目：
          * `gcloud auth application-default login`
          * `gcloud auth application-default set-quota-project [您的项目ID]`

2.  **项目文件**:

      * 所有代码文件 (`requirements.txt`, `register_prompts.py`, `promptlab_server.py`, `promptlab_client.py`) 均已按照我们之前的讨论更新为最终版本。
      * 项目根目录下已创建 `.env` 文件，并正确填写了您的 `GCP_PROJECT_ID` 和 `MLFLOW_TRACKING_URI`。

3.  **Python 依赖**:

      * 您已经在一个干净的虚拟环境 (`venv`) 中，根据更新后的 `requirements.txt` 文件，通过运行 `pip install -r requirements.txt` 安装了所有必需的库。

#### **二、 测试环境准备**

为了顺利完成测试，您需要同时打开 **3 个独立的终端窗口** (例如: PowerShell, CMD 或 Windows Terminal)。

  * **终端 1**: 用于运行 MLflow 服务。
  * **终端 2**: 用于注册提示词和运行 PromptLab 主服务器。
  * **终端 3**: 用于运行客户端并发送测试请求。

**操作**: 请打开 3 个终端，并在**每一个终端**中都执行以下命令，进入项目目录并激活虚拟环境：

```powershell
# 在每个终端中都执行
cd E:\User\MCP\PromptLab
.\venv\Scripts\Activate.ps1
```

*成功后，每个终端的命令行提示符前都应该有 `(venv)` 字样。*

-----

#### **三、 测试执行步骤**

请严格按照以下顺序执行。

##### **第 1 步：启动 MLflow 服务 (在终端 1)**

MLflow 是我们管理提示词模板的后端，必须首先启动。

1.  **执行命令**:

    ```bash
    # 在终端 1 中运行
    mlflow ui
    ```

2.  **验证**:

      * **终端验证**: 终端会显示 `INFO:waitress:Serving on http://127.0.0.1:5000`。
      * **浏览器验证**: 打开您的网络浏览器，访问 [可疑链接已删除]。您应该能看到 MLflow 的主界面。
      * **保持运行**: 请让这个终端窗口保持打开状态。

##### **第 2 步：注册提示词模板 (在终端 2)**

此步骤会将项目自带的示例模板注册到 MLflow 中。

1.  **执行命令**:

    ```bash
    # 在终端 2 中运行
    python register_prompts.py register-samples
    ```

2.  **验证**:

      * **终端验证**: 终端会输出一个JSON结果，其中所有条目的 `"status"` 都应为 `"success"`。
      * **浏览器验证**: 刷新浏览器中的 MLflow 页面，点击左侧导航栏的 **"Prompts"** 选项。您应该能看到一个包含 `essay_prompt`, `email_prompt` 等多个提示词的列表。

##### **第 3 步：启动 PromptLab 主服务器 (在终端 2)**

现在，我们将启动集成了 Vertex AI 的核心服务。

1.  **执行命令**:

    ```bash
    # 在终端 2 中继续运行
    python promptlab_server.py
    ```

2.  **验证 (关键步骤)**:
    请**仔细观察终端 2 的日志输出**，寻找以下两条**必须出现**的关键信息：

      * `INFO:promptlab-server:ChatVertexAI initialized for model 'gemini-2.5-pro' in project '[您的项目ID]'`
      * `INFO:promptlab-server:Loaded [数字] prompts from MLflow`

    看到这些日志代表服务器已成功连接 Google Cloud 并加载了 MLflow 数据。

      * **保持运行**: 请让这个终端窗口也保持打开状态，它正在等待客户端的请求。

##### **第 4 步：使用客户端发送请求 (在终端 3)**

最后，我们将模拟用户，向我们已经成功运行的服务器发送指令。

1.  **测试 1: 检查服务连通性**

      * **执行命令**:
        ```bash
        # 在终端 3 中运行
        python promptlab_client.py --list
        ```
      * **预期结果**: 终端会打印出一个 JSON 列表，内容是从服务器获取的、所有已注册的提示词信息。

2.  **测试 2: 验证端到端完整流程**

      * **执行命令**:
        ```bash
        # 在终端 3 中运行
        python promptlab_client.py --verbose "请帮我构思一个关于‘可持续城市发展’的演示文稿大纲"
        ```
      * **预期结果**:
          * **客户端 (终端 3)**: 会打印出一段经过美化的彩色输出。其中，`Enhanced Query` 字段的内容应该是一个被 Gemini 2.5 Pro 优化过的、非常详细的、结构化的新提示。`Matched Prompt` 字段应该会显示 `presentation_prompt`。
          * **服务器 (终端 2)**: 在您执行命令的瞬间，服务器的日志会**实时滚动**，出现 `Processing query through workflow...`, `Matching query...`, `Enhancing query...` 等一系列新的日志，这表明服务器正在调用 Gemini 模型处理您的请求。

-----

#### **四、 成功标准**

如果**第 4 步**中的两个测试都得到了预期的结果，那么：

**恭喜您！您的 PromptLab 工具已经成功在本地部署、配置，并能通过 Vertex AI 正确调用 Gemini 2.5 Pro 模型来完成任务。**