# 📋 PromptLab MCP Server 启动脚本说明

## 🎯 脚本概览

本项目提供了多种启动方式，以适应不同的操作系统和使用场景：

| 脚本文件 | 平台支持 | 推荐程度 | 特点 |
|---------|---------|---------|------|
| `start_mcp_server.py` | 全平台 | ⭐⭐⭐⭐⭐ | 功能最完整，错误处理最好 |
| `start_promptlab_mcp.ps1` | Windows | ⭐⭐⭐⭐ | PowerShell 原生，彩色输出 |
| `start_promptlab_mcp.bat` | Windows | ⭐⭐⭐ | 简单易用，兼容性好 |
| `promptlab_server.py` | 全平台 | ⭐⭐ | 直接启动，需手动检查 |

## 📝 详细说明

### 1. start_mcp_server.py（推荐）

**文件**: `start_mcp_server.py`

**特点**:
- ✅ 跨平台支持（Windows、macOS、Linux）
- ✅ 完整的环境检查（Python版本、虚拟环境、依赖包）
- ✅ 详细的错误信息和解决建议
- ✅ 自动检测操作系统并适配路径
- ✅ 优雅的错误处理和用户交互
- ✅ 支持超时检测和进程管理

**使用方法**:
```bash
python start_mcp_server.py
```

**适用场景**:
- 首次使用或不确定环境配置
- 需要详细的状态检查和错误诊断
- 跨平台开发环境
- 生产环境部署

**检查项目**:
1. Python 版本验证（≥3.8）
2. 虚拟环境存在性检查
3. 核心依赖包导入测试
4. 服务器文件完整性验证
5. 启动过程监控

---

### 2. start_promptlab_mcp.ps1

**文件**: `start_promptlab_mcp.ps1`

**特点**:
- ✅ Windows PowerShell 原生支持
- ✅ 彩色输出，用户体验好
- ✅ 详细的状态信息显示
- ✅ 异常处理和错误捕获
- ✅ UTF-8 编码支持
- ⚠️ 仅支持 Windows 系统

**使用方法**:
```powershell
.\start_promptlab_mcp.ps1
```

**适用场景**:
- Windows 开发环境
- 喜欢 PowerShell 的用户
- 需要彩色输出和美观界面
- 企业 Windows 环境

**权限要求**:
```powershell
# 如果遇到执行策略问题，运行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**检查项目**:
1. 当前目录显示
2. 虚拟环境检查
3. 环境激活验证
4. 依赖包导入测试
5. 服务器启动监控

---

### 3. start_promptlab_mcp.bat

**文件**: `start_promptlab_mcp.bat`

**特点**:
- ✅ Windows 批处理，兼容性最好
- ✅ 无需额外权限配置
- ✅ 简单直接，易于理解
- ✅ 支持中文显示（UTF-8）
- ⚠️ 错误处理相对简单
- ⚠️ 仅支持 Windows 系统

**使用方法**:
```cmd
start_promptlab_mcp.bat
```

**适用场景**:
- Windows 传统环境
- 不允许运行 PowerShell 脚本的环境
- 简单快速启动需求
- 批处理脚本集成

**检查项目**:
1. 虚拟环境存在性
2. 环境激活
3. 基础依赖检查
4. 服务器启动

---

### 4. promptlab_server.py（直接启动）

**文件**: `promptlab_server.py`

**特点**:
- ✅ 直接启动，无额外检查
- ✅ 最快的启动方式
- ✅ 适合自动化脚本
- ⚠️ 需要手动确保环境正确
- ⚠️ 错误信息可能不够详细

**使用方法**:
```bash
# 需要先激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 然后启动
python promptlab_server.py
```

**适用场景**:
- 环境已确认正确
- 自动化部署脚本
- 容器化部署
- 高级用户直接控制

---

## 🔧 选择建议

### 新手用户
推荐使用 `start_mcp_server.py`：
```bash
python start_mcp_server.py
```

### Windows 用户
推荐使用 `start_promptlab_mcp.ps1`：
```powershell
.\start_promptlab_mcp.ps1
```

### 快速启动
推荐使用 `start_promptlab_mcp.bat`：
```cmd
start_promptlab_mcp.bat
```

### 高级用户
直接使用 `promptlab_server.py`：
```bash
python promptlab_server.py
```

## 🐛 故障排除

### 通用问题

1. **虚拟环境不存在**
   ```bash
   python setup_and_start.py
   ```

2. **依赖包问题**
   ```bash
   pip install -r requirements.txt
   ```

3. **权限问题（Windows）**
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

### 脚本特定问题

#### PowerShell 脚本
- 确保执行策略允许脚本运行
- 检查 PowerShell 版本（建议 5.1+）

#### 批处理脚本
- 确保在正确的目录中运行
- 检查路径中是否包含特殊字符

#### Python 脚本
- 确保 Python 版本 ≥ 3.8
- 检查 PATH 环境变量

## 📊 性能对比

| 特性 | Python脚本 | PowerShell | 批处理 | 直接启动 |
|------|-----------|-----------|--------|----------|
| 启动速度 | 中等 | 快 | 最快 | 最快 |
| 错误检查 | 最完整 | 完整 | 基础 | 无 |
| 跨平台 | ✅ | ❌ | ❌ | ✅ |
| 用户友好 | 最好 | 好 | 一般 | 差 |
| 维护性 | 最好 | 好 | 一般 | 好 |

## 🎯 最佳实践

1. **开发阶段**: 使用 `start_mcp_server.py` 进行完整检查
2. **日常使用**: 使用平台特定脚本（`.ps1` 或 `.bat`）
3. **生产部署**: 使用 `promptlab_server.py` 直接启动
4. **自动化**: 集成到 CI/CD 流程中使用直接启动

---

**💡 提示**: 选择最适合你环境和需求的启动方式，所有脚本都会启动相同的 MCP 服务器！