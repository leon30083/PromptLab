# PromptLab 手动启动说明

本文档提供了PromptLab MCP服务器的详细启动指南，包括本地启动和AWS EC2部署两种方案。

## 📋 目录

- [本地启动指南](#本地启动指南)
- [AWS EC2部署指南](#aws-ec2部署指南)
- [故障排除](#故障排除)
- [常见问题](#常见问题)

---

## 🏠 本地启动指南

### 前置要求

- Python 3.8+
- Git
- 有效的API密钥（OpenAI、Anthropic、Google等）

### 步骤1：环境准备

```bash
# 1. 导航到项目目录
cd e:\User\MCP\PromptLab

# 2. 创建虚拟环境（如果还没有）
python -m venv venv

# 3. 激活虚拟环境
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Windows CMD:
venv\Scripts\activate.bat

# 4. 安装依赖
pip install -r requirements.txt
```

### 步骤2：配置环境变量

创建 `.env` 文件（如果不存在）：

```bash
# API密钥配置
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# MLflow配置
MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# 服务器配置
PROMPTLAB_HOST=127.0.0.1
PROMPTLAB_PORT=8000
```

### 步骤3：启动服务

**重要：需要使用两个终端窗口**

#### 终端1：启动MLflow服务器

```bash
# 导航到项目目录
cd e:\User\MCP\PromptLab

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 启动MLflow服务器
mlflow server --host 127.0.0.1 --port 5000 --backend-store-uri file:///e:/User/MCP/PromptLab/mlruns
```

**保持此终端窗口打开！**

#### 终端2：启动PromptLab服务器

```bash
# 导航到项目目录
cd e:\User\MCP\PromptLab

# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 启动PromptLab服务器
python enhanced_promptlab_server.py
```

**保持此终端窗口打开！**

### 步骤4：验证服务状态

1. **MLflow UI**: 访问 http://127.0.0.1:5000
2. **PromptLab状态**: 检查终端2的输出，应显示"PromptLab MCP Server started"

### 步骤5：配置Trae AI

在Trae AI中配置MCP连接：

```json
{
  "mcpServers": {
    "promptlab": {
      "command": "python",
      "args": ["e:\\User\\MCP\\PromptLab\\enhanced_promptlab_server.py"],
      "cwd": "e:\\User\\MCP\\PromptLab",
      "env": {
        "OPENAI_API_KEY": "your_key_here",
        "ANTHROPIC_API_KEY": "your_key_here"
      }
    }
  }
}
```

---

## ☁️ AWS EC2部署指南

### 前置要求

- AWS账户（免费套餐）
- EC2实例（推荐t2.micro）
- 基本的Linux命令知识

### 步骤1：EC2实例配置

**推荐配置：**
- 实例类型：t2.micro（免费套餐）
- 操作系统：Ubuntu 22.04 LTS
- 存储：8GB（免费套餐）
- 安全组：开放端口22（SSH）、5000（MLflow）、8000（PromptLab）

### 步骤2：连接到EC2实例

```bash
# 使用SSH连接到EC2实例
ssh -i your-key.pem ubuntu@your-ec2-public-ip
```

### 步骤3：服务器环境配置

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python和必要工具
sudo apt install python3 python3-pip python3-venv git nginx -y

# 上传或克隆PromptLab项目
# 方法1：使用git（如果项目在GitHub上）
git clone https://github.com/your-username/PromptLab.git

# 方法2：使用scp上传本地文件
# scp -i your-key.pem -r e:\User\MCP\PromptLab ubuntu@your-ec2-ip:~/

# 进入项目目录
cd PromptLab

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 步骤4：配置环境变量

```bash
# 创建环境配置文件
nano .env
```

添加以下内容：
```bash
# API密钥配置
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
GOOGLE_API_KEY=your_google_key

# 服务器配置
MLFLOW_TRACKING_URI=http://localhost:5000
PROMPTLAB_HOST=0.0.0.0
PROMPTLAB_PORT=8000
```

### 步骤5：配置系统服务

#### 创建MLflow服务

```bash
sudo nano /etc/systemd/system/mlflow.service
```

```ini
[Unit]
Description=MLflow Tracking Server
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/PromptLab
Environment=PATH=/home/ubuntu/PromptLab/venv/bin
ExecStart=/home/ubuntu/PromptLab/venv/bin/mlflow server --host 0.0.0.0 --port 5000
Restart=always

[Install]
WantedBy=multi-user.target
```

#### 创建PromptLab服务

```bash
sudo nano /etc/systemd/system/promptlab.service
```

```ini
[Unit]
Description=PromptLab MCP Server
After=network.target mlflow.service
Requires=mlflow.service

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/PromptLab
Environment=PATH=/home/ubuntu/PromptLab/venv/bin
EnvironmentFile=/home/ubuntu/PromptLab/.env
ExecStart=/home/ubuntu/PromptLab/venv/bin/python enhanced_promptlab_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### 步骤6：启动服务

```bash
# 重新加载systemd配置
sudo systemctl daemon-reload

# 启用并启动服务
sudo systemctl enable mlflow promptlab
sudo systemctl start mlflow promptlab

# 检查服务状态
sudo systemctl status mlflow
sudo systemctl status promptlab
```

### 步骤7：配置Nginx反向代理（可选）

```bash
sudo nano /etc/nginx/sites-available/promptlab
```

```nginx
server {
    listen 80;
    server_name your-ec2-public-ip;

    location /mlflow/ {
        proxy_pass http://localhost:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

```bash
# 启用站点
sudo ln -s /etc/nginx/sites-available/promptlab /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 步骤8：验证部署

```bash
# 检查服务状态
curl http://localhost:5000
curl http://localhost:8000

# 查看日志
sudo journalctl -u mlflow -f
sudo journalctl -u promptlab -f
```

### 步骤9：配置Trae AI（远程连接）

```json
{
  "mcpServers": {
    "promptlab": {
      "command": "ssh",
      "args": [
        "ubuntu@your-ec2-public-ip",
        "cd /home/ubuntu/PromptLab && source venv/bin/activate && python enhanced_promptlab_server.py"
      ],
      "env": {
        "OPENAI_API_KEY": "your_key",
        "ANTHROPIC_API_KEY": "your_key"
      }
    }
  }
}
```

---

## 🔧 故障排除

### 常见问题及解决方案

#### 1. MLflow启动失败

**错误**: `FileNotFoundError: mlruns`

**解决方案**:
```bash
# 创建mlruns目录
mkdir mlruns

# 或使用绝对路径启动
mlflow server --backend-store-uri file:///absolute/path/to/mlruns
```

#### 2. 端口被占用

**错误**: `Address already in use`

**解决方案**:
```bash
# Windows - 查找占用端口的进程
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# Linux - 查找并终止进程
sudo lsof -i :5000
sudo kill -9 <PID>
```

#### 3. 虚拟环境问题

**错误**: 模块未找到

**解决方案**:
```bash
# 确认虚拟环境已激活
which python  # 应显示venv路径

# 重新安装依赖
pip install -r requirements.txt
```

#### 4. API密钥问题

**错误**: `Authentication failed`

**解决方案**:
- 检查 `.env` 文件中的API密钥是否正确
- 确认API密钥有足够的配额
- 验证环境变量是否正确加载

#### 5. 权限问题（Linux）

**错误**: `Permission denied`

**解决方案**:
```bash
# 修改文件权限
chmod +x enhanced_promptlab_server.py

# 或使用sudo运行systemctl命令
sudo systemctl restart promptlab
```

### 日志查看

#### 本地环境
- 查看终端输出
- 检查 `mlruns` 目录下的日志文件

#### EC2环境
```bash
# 查看系统服务日志
sudo journalctl -u mlflow -f
sudo journalctl -u promptlab -f

# 查看Nginx日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

---

## ❓ 常见问题

### Q1: 如何更新PromptLab？

**本地环境**:
```bash
git pull origin main
pip install -r requirements.txt
```

**EC2环境**:
```bash
git pull origin main
sudo systemctl restart promptlab
```

### Q2: 如何备份MLflow数据？

```bash
# 备份mlruns目录
tar -czf mlruns_backup_$(date +%Y%m%d).tar.gz mlruns/

# 恢复备份
tar -xzf mlruns_backup_YYYYMMDD.tar.gz
```

### Q3: 如何监控服务状态？

**本地环境**:
- 访问 http://127.0.0.1:5000 查看MLflow UI
- 检查终端输出

**EC2环境**:
```bash
# 检查服务状态
sudo systemctl status mlflow promptlab

# 监控资源使用
htop
df -h
```

### Q4: 如何配置HTTPS？

**使用Let's Encrypt**:
```bash
# 安装certbot
sudo apt install certbot python3-certbot-nginx

# 获取SSL证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo crontab -e
# 添加: 0 12 * * * /usr/bin/certbot renew --quiet
```

### Q5: 如何扩展到多个实例？

- 使用负载均衡器（如AWS ALB）
- 配置共享存储（如AWS EFS）
- 使用容器化部署（Docker + Kubernetes）

---

## 📞 支持

如果遇到问题，请：

1. 检查本文档的故障排除部分
2. 查看服务日志
3. 确认所有依赖都已正确安装
4. 验证网络连接和防火墙设置

---

**最后更新**: 2024年12月
**版本**: 1.0
**适用于**: PromptLab MCP Server v1.0+