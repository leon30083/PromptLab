# PromptLab MCP Server 使用说明

## 📋 目录

- [项目简介](#项目简介)
- [系统要求](#系统要求)
- [安装步骤](#安装步骤)
- [启动方式](#启动方式)
- [配置说明](#配置说明)
- [功能介绍](#功能介绍)
- [故障排除](#故障排除)
- [常见问题](#常见问题)

## 🎯 项目简介

PromptLab MCP Server 是一个基于 Model Context Protocol (MCP) 的 AI 提示词管理工具，集成了以下功能：

- **智能提示词优化**: 使用 Google Gemini 2.5 Pro 优化用户查询
- **提示词模板管理**: 基于 MLflow 的提示词注册和版本控制
- **多模态支持**: 支持文本和图像输入的提示词处理
- **MCP 标准**: 完全兼容 MCP 协议，可与各种 AI 客户端集成

## 💻 系统要求

### 基础要求
- **操作系统**: Windows 10/11, macOS 10.15+, Linux (Ubuntu 18.04+)
- **Python**: 3.8 或更高版本
- **内存**: 至少 4GB RAM
- **存储**: 至少 2GB 可用空间

### 依赖服务
- **Google Cloud**: 需要 Vertex AI 访问权限
- **MLflow**: 用于提示词管理（可选，本地运行）

## 🚀 安装步骤

### 步骤 1: 克隆项目
```bash
git clone <repository-url>
cd PromptLab
```

### 步骤 2: 自动安装
运行自动安装脚本：
```bash
python setup_and_start.py
```

这个脚本会自动：
- 创建虚拟环境
- 安装所有依赖包
- 配置环境变量
- 验证安装

### 步骤 3: 手动安装（可选）
如果自动安装失败，可以手动安装：

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

## 🎮 启动方式

### 方式 1: 使用 Python 脚本（推荐）
```bash
python start_mcp_server.py
```

**特点**:
- 跨平台支持
- 完整的错误检查
- 详细的状态信息
- 自动依赖验证

### 方式 2: 使用 PowerShell 脚本 (Windows)
```powershell
.\start_promptlab_mcp.ps1
```

**特点**:
- Windows 原生支持
- 彩色输出
- 详细的错误信息

### 方式 3: 使用批处理文件 (Windows)
```cmd
start_promptlab_mcp.bat
```

**特点**:
- 简单易用
- 兼容性好
- 适合快速启动

### 方式 4: 直接启动
```bash
# 激活虚拟环境后
python promptlab_server.py
```

## ⚙️ 配置说明

### MCP 客户端配置

#### Trae AI 配置
使用 `mcp_trae.json` 配置文件：

```json
{
  "mcpServers": {
    "promptlab": {
      "command": "e:\\User\\MCP\\PromptLab\\venv\\Scripts\\python.exe",
      "args": ["e:\\User\\MCP\\PromptLab\\promptlab_server.py"],
      "cwd": "e:\\User\\MCP\\PromptLab",
      "env": {
        "PYTHONPATH": "e:\\User\\MCP\\PromptLab",
        "VIRTUAL_ENV": "e:\\User\\MCP\\PromptLab\\venv",
        "MLFLOW_TRACKING_URI": "http://127.0.0.1:5000",
        "PROMPTLAB_DEBUG": "false"
      },
      "timeout": 30000,
      "restart": true
    }
  }
}
```

#### 通用 MCP 配置
使用 `mcp.json` 配置文件：

```json
{
  "mcpServers": {
    "promptlab": {
      "command": "python",
      "args": ["promptlab_server.py"],
      "cwd": "./"
    }
  }
}
```

### 环境变量配置

创建 `.env` 文件（可选）：
```env
# Google Cloud 配置
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# MLflow 配置
MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# 调试模式
PROMPTLAB_DEBUG=false
```

## 🛠️ 功能介绍

### 1. optimize_query
**功能**: 优化用户查询，匹配最佳提示词模板

**使用示例**:
```python
# 通过 MCP 客户端调用
result = mcp_client.call_tool("optimize_query", {
    "query": "帮我写一个Python函数"
})
```

**参数**:
- `query` (string): 需要优化的用户查询

**返回**: 优化后的查询和匹配的模板信息

### 2. list_prompts
**功能**: 列出所有可用的提示词模板

**使用示例**:
```python
result = mcp_client.call_tool("list_prompts", {})
```

**返回**: 所有注册的提示词模板列表

### 3. reload_prompts
**功能**: 重新加载提示词模板

**使用示例**:
```python
result = mcp_client.call_tool("reload_prompts", {})
```

**返回**: 重新加载的状态信息

## 🔧 MLflow 集成

### 启动 MLflow 服务器

**重要**: MLflow 服务器需要在单独的终端中启动

```bash
# 在新的终端窗口中运行
cd e:\User\MCP\PromptLab
.\venv\Scripts\Activate.ps1
mlflow server --host 127.0.0.1 --port 5000
```

### 注册示例提示词

```bash
# 在另一个终端中运行
python register_prompts.py
```

### 访问 MLflow UI

启动 MLflow 服务器后，访问: http://127.0.0.1:5000

## 🐛 故障排除

### 常见错误及解决方案

#### 1. 导入错误
```
ValueError: numpy.dtype size changed
```

**解决方案**:
```bash
pip install numpy==1.26.4 pandas==2.2.3
```

#### 2. 虚拟环境问题
```
虚拟环境不存在或无法激活
```

**解决方案**:
```bash
# 重新创建虚拟环境
rmdir /s venv  # Windows
rm -rf venv    # macOS/Linux
python -m venv venv
```

#### 3. 权限问题 (Windows)
```
Execution of scripts is disabled on this system
```

**解决方案**:
```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### 4. 端口占用
```
MLflow 服务器启动失败，端口被占用
```

**解决方案**:
```bash
# 查找占用端口的进程
netstat -ano | findstr :5000  # Windows
lsof -i :5000                 # macOS/Linux

# 或使用其他端口
mlflow server --host 127.0.0.1 --port 5001
```

### 调试模式

启用调试模式获取更多信息：

```bash
# 设置环境变量
set PROMPTLAB_DEBUG=true  # Windows
export PROMPTLAB_DEBUG=true  # macOS/Linux

# 或在 .env 文件中设置
PROMPTLAB_DEBUG=true
```

## ❓ 常见问题

### Q1: 如何更新依赖包？
```bash
pip install -r requirements.txt --upgrade
```

### Q2: 如何备份提示词数据？
MLflow 数据存储在 `mlruns` 目录中，备份该目录即可。

### Q3: 如何添加自定义提示词？
1. 修改 `register_prompts.py` 文件
2. 添加新的提示词模板
3. 运行 `python register_prompts.py`

### Q4: 如何在不同端口运行？
修改环境变量 `MLFLOW_TRACKING_URI`：
```bash
set MLFLOW_TRACKING_URI=http://127.0.0.1:5001
```

### Q5: 如何集成到其他 MCP 客户端？
1. 复制相应的配置文件（`mcp.json` 或 `mcp_trae.json`）
2. 修改路径为你的实际路径
3. 在客户端中加载配置

## 📞 技术支持

如果遇到问题：

1. **检查日志**: 查看服务器输出的错误信息
2. **验证环境**: 确保所有依赖正确安装
3. **查看文档**: 参考本使用说明和故障排除部分
4. **调试模式**: 启用 `PROMPTLAB_DEBUG=true` 获取详细信息

## 📝 更新日志

- **v1.0.0**: 初始版本，支持基础 MCP 功能
- **v1.1.0**: 添加 MLflow 集成和提示词管理
- **v1.2.0**: 优化启动脚本和错误处理
- **v1.3.0**: 添加 Trae AI 支持和配置优化

---

**注意**: 本文档会随着项目更新而更新，请定期查看最新版本。